const init = [
    {
        _id:'1',
        name:"Đông trùng hạ thảo"
    }
]

const category = (state=init,action)=>{
    switch(action.type){
        default:
            return state
    }
}

export default category